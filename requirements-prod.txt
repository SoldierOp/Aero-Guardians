# PeatGuard Production Requirements (Lightweight)
# Excludes heavy ML training libraries

# Backend API
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
psycopg2-binary==2.9.10
paho-mqtt==2.1.0
twilio==9.3.7
python-dotenv==1.0.1
python-multipart==0.0.9

# Dashboard
streamlit==1.41.1
plotly==5.24.1

# Data Processing
pandas==2.2.3
numpy==2.1.3
requests==2.32.3
colorama==0.4.6

# ML Inference Only (Lightweight - no TensorFlow)
scikit-learn==1.3.2
joblib==1.3.2

# Note: TensorFlow removed for production (model already trained)
# Model files (.h5, .tflite) are pre-trained and included in repo
